{
  "name": "messengerApp",
  "version": "1.0.0",
  "description": "messenger app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Edwin Ochoa",
  "license": "ISC",
  "dependencies": {
    "body-parser": "*",
    "express": "*",
    "socket.io": "*",
    "request":"*"
  }
}
